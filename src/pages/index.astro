---
// Root (/) → default locale. Indexable pages live under /[lang]/; this required
// index route redirects the bare root to /ro (i18n redirectToDefaultLocale).
export const prerender = false;

return Astro.redirect('/ro');
---
